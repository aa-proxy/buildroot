#!/bin/sh
#
# Load kernel modules (non-blocking)
#

case "$1" in
	start)
		echo "Loading kernel modules..."
		modprobe -a aic8800_bsp aic8800_fdrv libcomposite &
		;;
	*)
		echo "Usage: $0 {start}"
		exit 1
esac

exit $?
